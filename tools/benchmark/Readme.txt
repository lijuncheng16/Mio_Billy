This directory contains scripts that can benchmark an ejabber 

Originally used to text Ejabberd as well as Prosody to compare. 
Will be generalized to pubsub.

To build:

To run:


The results:
The results are marked in messages/second the server is handling. This is the dependant variable.
The independent variables include number of users, number of subscribers, number of event nodes, and published message size.

 
